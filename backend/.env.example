# Copy this file to .env and fill in real values.
# If GEMINI_API_KEY is left blank, the system automatically runs in
# offline stub mode (see app/config.py: USE_LLM_STUB) so the full
# pipeline remains runnable without an API key.

GEMINI_API_KEY=
GEMINI_MODEL_NAME=gemini-1.5-flash
